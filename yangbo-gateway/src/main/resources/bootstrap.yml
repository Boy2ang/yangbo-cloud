spring:
  application:
    name: YangBo-Gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8001
      config:
        server-addr: 127.0.0.1:8001
        group: DEFAULT_GROUP
        prefix: yangbo-gateway
        # 指定微服务配置文件的类型
        file-extension: yaml
